<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Manual Caser® Digital - Visor por Páginas</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }
        
        body {
            background: linear-gradient(135deg, #6a11cb 0%, #2575fc 100%);
            color: #333;
            min-height: 100vh;
            padding: 20px;
        }
        
        .container {
            max-width: 1200px;
            margin: 0 auto;
        }
        
        header {
            text-align: center;
            padding: 30px 0;
            color: white;
        }
        
        header h1 {
            font-size: 2.5rem;
            margin-bottom: 10px;
            text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.3);
        }
        
        header p {
            font-size: 1.2rem;
            opacity: 0.9;
        }
        
        .content {
            display: flex;
            gap: 30px;
            margin-bottom: 30px;
        }
        
        .pdf-section {
            flex: 3;
        }
        
        .info-panel {
            flex: 1;
            background: white;
            border-radius: 15px;
            padding: 25px;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.15);
        }
        
        .info-panel h2 {
            color: #2575fc;
            margin-bottom: 20px;
            padding-bottom: 10px;
            border-bottom: 2px solid #f0f0f0;
        }
        
        .info-item {
            margin-bottom: 20px;
        }
        
        .info-item h3 {
            color: #6a11cb;
            margin-bottom: 8px;
            font-size: 1.1rem;
        }
        
        .pdf-viewer-container {
            background: white;
            border-radius: 15px;
            overflow: hidden;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.15);
        }
        
        .pdf-header {
            background: linear-gradient(to right, #6a11cb, #2575fc);
            color: white;
            padding: 20px;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }
        
        .pdf-header h2 {
            font-size: 1.5rem;
        }
        
        .page-indicator {
            background: rgba(255, 255, 255, 0.2);
            padding: 8px 15px;
            border-radius: 20px;
            font-weight: bold;
        }
        
        .pdf-frame-container {
            padding: 20px;
            background: #f8f9fa;
            text-align: center;
        }
        
        .pdf-iframe {
            width: 100%;
            height: 600px;
            border: none;
            border-radius: 10px;
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.1);
        }
        
        .pdf-actions {
            padding: 20px;
            display: flex;
            justify-content: space-between;
            background: white;
            border-top: 1px solid #eee;
        }
        
        .btn {
            padding: 12px 25px;
            border: none;
            border-radius: 8px;
            font-weight: bold;
            cursor: pointer;
            transition: all 0.3s ease;
            display: inline-flex;
            align-items: center;
            gap: 8px;
        }
        
        .btn-primary {
            background: #2575fc;
            color: white;
        }
        
        .btn-primary:hover {
            background: #1a68e3;
            transform: translateY(-2px);
        }
        
        .btn-secondary {
            background: #6a11cb;
            color: white;
        }
        
        .btn-secondary:hover {
            background: #580da8;
            transform: translateY(-2px);
        }
        
        .btn-outline {
            background: transparent;
            border: 2px solid #2575fc;
            color: #2575fc;
        }
        
        .btn-outline:hover {
            background: #2575fc;
            color: white;
        }
        
        .btn:disabled {
            background: #cccccc;
            cursor: not-allowed;
            transform: none;
        }
        
        .navigation {
            display: flex;
            justify-content: center;
            gap: 15px;
            margin-top: 20px;
        }
        
        .page-controls {
            display: flex;
            justify-content: center;
            align-items: center;
            gap: 15px;
            margin: 20px 0;
        }
        
        .page-input {
            width: 60px;
            padding: 8px;
            text-align: center;
            border: 1px solid #ddd;
            border-radius: 5px;
        }
        
        footer {
            text-align: center;
            padding: 20px;
            color: white;
            margin-top: 30px;
            opacity: 0.8;
        }
        
        @media (max-width: 900px) {
            .content {
                flex-direction: column;
            }
            
            .pdf-iframe {
                height: 500px;
            }
        }
        
        @media (max-width: 600px) {
            .pdf-iframe {
                height: 400px;
            }
            
            .pdf-actions {
                flex-direction: column;
                gap: 10px;
            }
            
            .btn {
                width: 100%;
                justify-content: center;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <header>
            <h1>Manual Caser® Digital</h1>
            <p>Guía completa para el uso de la plataforma digital</p>
        </header>
        
        <div class="content">
            <section class="pdf-section">
                <div class="pdf-viewer-container">
                    <div class="pdf-header">
                        <h2>📋 Manual del Usuario</h2>
                        <div class="page-indicator">
                            Página <span id="current-page">1</span> de <span id="total-pages">72</span>
                        </div>
                    </div>
                    
                    <div class="pdf-frame-container">
                        <!-- IFRAME DEL PDF - Se actualizará con JavaScript -->
                        <iframe 
                            class="pdf-iframe"
                            src="https://drive.google.com/file/d/1sVmvxT27V9eo-Ng3T_54cMvbSDk4Mqqe/preview"
                            id="pdf-iframe">
                        </iframe>
                    </div>
                    
                    <div class="page-controls">
                        <button class="btn btn-secondary" id="prev-btn" onclick="changePage(-1)">
                            ← Anterior
                        </button>
                        
                        <div>
                            <input type="number" id="page-input" class="page-input" min="1" max="72" value="1">
                            <span> / 72</span>
                        </div>
                        
                        <button class="btn btn-primary" id="next-btn" onclick="changePage(1)">
                            Siguiente →
                        </button>
                    </div>
                    
                    <div class="pdf-actions">
                        <button class="btn btn-outline" onclick="downloadPDF()">
                            📥 Descargar PDF
                        </button>
                        
                        <button class="btn btn-outline" onclick="openInNewTab()">
                            🔗 Abrir en nueva pestaña
                        </button>
                    </div>
                </div>
                
                <div class="navigation">
                    <button class="btn btn-outline" onclick="goToPage('index.html')">
                        🏠 Inicio
                    </button>
                    <button class="btn btn-primary" onclick="goToPage('test.html')">
                        ✏️ Realizar Test
                    </button>
                </div>
            </section>
            
            <aside class="info-panel">
                <h2>Navegación por Páginas</h2>
                
                <div class="info-item">
                    <h3>📄 Cómo navegar</h3>
                    <p>Usa los botones <strong>Anterior</strong> y <strong>Siguiente</strong> para moverte página por página a través del manual.</p>
                </div>
                
                <div class="info-item">
                    <h3>🔢 Ir a página específica</h3>
                    <p>Escribe el número de página en el campo de texto y presiona Enter para saltar directamente a esa página.</p>
                </div>
                
                <div class="info-item">
                    <h3>📖 Contenido del manual</h3>
                    <ul style="margin-left: 20px; margin-top: 8px;">
                        <li>Páginas 1-10: Introducción</li>
                        <li>Páginas 11-25: Configuración</li>
                        <li>Páginas 26-45: Funcionalidades</li>
                        <li>Páginas 46-60: Gestión de usuarios</li>
                        <li>Páginas 61-72: Resolución de problemas</li>
                    </ul>
                </div>
                
                <div class="info-item">
                    <h3>💡 Consejos</h3>
                    <p>Para una mejor experiencia, mantén el manual abierto en una pestaña separada mientras realizas el test.</p>
                </div>
            </aside>
        </div>
        
        <footer>
            <p>Manual Caser® Digital &copy; 2023 - Todos los derechos reservados</p>
        </footer>
    </div>

    <script>
        // Variables para controlar la página actual
        let currentPage = 1;
        const totalPages = 72;
        
        // Elementos del DOM
        const pdfIframe = document.getElementById('pdf-iframe');
        const currentPageElement = document.getElementById('current-page');
        const pageInput = document.getElementById('page-input');
        const prevBtn = document.getElementById('prev-btn');
        const nextBtn = document.getElementById('next-btn');
        
        // Inicializar controles
        document.addEventListener('DOMContentLoaded', function() {
            updateControls();
        });
        
        // Función para cambiar de página
        function changePage(direction) {
            const newPage = currentPage + direction;
            
            if (newPage >= 1 && newPage <= totalPages) {
                currentPage = newPage;
                updatePDFViewer();
                updateControls();
            }
        }
        
        // Función para actualizar el visor del PDF
        function updatePDFViewer() {
            pdfIframe.src = `MANUAL DEL CASER@ DIGITAL REDUCIDO (1).docx.pdf#page=${currentPage}`;
            currentPageElement.textContent = currentPage;
            pageInput.value = currentPage;
        }
        
        // Función para actualizar los controles
        function updateControls() {
            // Actualizar estado de los botones
            prevBtn.disabled = currentPage <= 1;
            nextBtn.disabled = currentPage >= totalPages;
            
            // Actualizar campo de entrada
            pageInput.value = currentPage;
        }
        
        // Función para ir a una página específica
        pageInput.addEventListener('change', function() {
            let pageNum = parseInt(pageInput.value);
            
            if (isNaN(pageNum) || pageNum < 1) {
                pageNum = 1;
            } else if (pageNum > totalPages) {
                pageNum = totalPages;
            }
            
            currentPage = pageNum;
            updatePDFViewer();
            updateControls();
        });
        
        // Función para descargar el PDF
        function downloadPDF() {
            const link = document.createElement('a');
            link.href = 'MANUAL DEL CASER@ DIGITAL REDUCIDO (1).docx.pdf';
            link.download = 'Manual_Caser_Digital.pdf';
            document.body.appendChild(link);
            link.click();
            document.body.removeChild(link);
        }
        
        // Función para abrir en nueva pestaña
        function openInNewTab() {
            window.open('MANUAL DEL CASER@ DIGITAL REDUCIDO (1).docx.pdf', '_blank');
        }
        
        // Función para navegar a otras páginas (simulada)
        function goToPage(url) {
            alert(`En una implementación real, navegaría a: ${url}`);
            // window.location.href = url;
        }
    </script>
</body>
</html>
